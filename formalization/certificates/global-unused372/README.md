# Frozen Node 372

Import `QiushiGlobalOrbitUnused372` for
`QiushiMatmul.GlobalOrbit.Unused.rep372`. The concrete theorem
`QiushiMatmul.plane372UnusedGen_lb17` proves
`QuotientRankAtLeast (spanCodes [298,224,20,1]) 17` without premises.

There are 37 checked source rows, 5 dead rows, 26 live cosets and two exact
Farkas leaves after one exhaustive integer split. All source bounds are
closed. See [the batch record](../global-unused371/README.md) for reproduction,
scope and verification; this package's `verification.json` records its hashes.
