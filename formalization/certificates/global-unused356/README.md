# Node 356

`QiushiGlobalOrbitUnused356` exports
`QiushiMatmul.GlobalOrbit.Unused.rep356`, binding the actual frozen basis and
label. The concrete theorem `QiushiMatmul.plane356UnusedGen_lb17` proves:

```lean
QuotientRankAtLeast (spanCodes [290,96,20,1]) 17
```

It uses 27 closed source rows, 11 dead witnesses, 20 live cosets and one exact
Farkas leaf with right-hand side -2. Unavailable discovery candidate 136 is
not a proof dependency. No other open bound is assumed.

See [the batch documentation](../global-unused354/README.md) for replay,
finite checks, source provenance and the precise proof scope.
