# Node 355

`QiushiGlobalOrbitUnused355` exports
`QiushiMatmul.GlobalOrbit.Unused.rep355`, binding the actual frozen basis and
label. The concrete theorem `QiushiMatmul.plane355UnusedGen_lb17` proves:

```lean
QuotientRankAtLeast (spanCodes [164,96,20,1]) 17
```

It uses 21 closed source rows, nine dead witnesses, 22 live cosets and one
exact Farkas leaf with right-hand side -2. Unavailable discovery candidates
170, 173, 200 and 226 are not proof dependencies. No other open bound is assumed.

See [the batch documentation](../global-unused354/README.md) for replay,
finite checks, source provenance and the precise proof scope.
