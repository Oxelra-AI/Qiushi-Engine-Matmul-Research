# Frozen Node 383

Import `QiushiGlobalOrbitUnused383` for
`QiushiMatmul.GlobalOrbit.Unused.rep383`. The concrete theorem
`QiushiMatmul.plane383UnusedGen_lb17` proves
`QuotientRankAtLeast (spanCodes [289,68,19,10]) 17` without premises.

There are 28 source rows, 19 dead rows, 12 live cosets and one exact leaf with
RHS -14. All sources are closed. See
[the batch record](../global-unused382/README.md) for reproduction, scope and
verification; `verification.json` records the actual per-node build hashes.
