# Frozen Node 376

Import `QiushiGlobalOrbitUnused376` for
`QiushiMatmul.GlobalOrbit.Unused.rep376`. The concrete theorem
`QiushiMatmul.plane376UnusedGen_lb17` proves
`QuotientRankAtLeast (spanCodes [160,68,19,10]) 17` without premises.

There are 15 checked source rows, 10 dead rows, 21 live cosets and one exact
Farkas leaf. Its only predecessor nodes are 14, 29, 74, 202 and 239, all closed.
See [the batch record](../global-unused371/README.md) for reproduction, scope
and verification; this package's `verification.json` records its hashes.
