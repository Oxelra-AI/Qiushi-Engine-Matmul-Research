# Frozen Node 380

Import `QiushiGlobalOrbitUnused380` for
`QiushiMatmul.GlobalOrbit.Unused.rep380`. The concrete theorem
`QiushiMatmul.plane380UnusedGen_lb17` proves
`QuotientRankAtLeast (spanCodes [258,68,19,10]) 17` without premises.

There are 26 source rows, 21 dead rows, 10 live cosets and one exact leaf with
RHS -6. All sources are closed. See
[the batch record](../global-unused378/README.md) for reproduction, scope and
verification; `verification.json` records the actual per-node build hashes.
