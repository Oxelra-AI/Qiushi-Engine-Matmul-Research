# Node 307

Import `QiushiGlobalOrbitUnused307` for
`QiushiMatmul.GlobalOrbit.Unused.rep307`, the exact frozen-table rank binding.
The concrete theorem `QiushiMatmul.plane307UnusedGen_lb17` proves
`QuotientRankAtLeast (spanCodes [272,96,10,1]) 17`.

The certificate has 20 closed source bounds, 12 dead witnesses, 19 live cosets,
and one exact Farkas leaf with right-hand side -3. Every selected source is
already proved. In particular, unbound discovery candidates 170 and 200 are
not used. There is no dependency on nodes 305 or 306 and no registry import.

See [the batch documentation](../global-unused305/README.md) for reproduction,
scope, block width, negative controls and the exact numerical proof boundary.
